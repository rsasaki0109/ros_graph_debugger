<!-- Thanks for contributing! Keep PRs focused; one coherent change is easiest to review. -->

## What & why

<!-- What does this change, and what problem does it solve? -->

## How it was tested

<!-- e.g. `python3 -m pytest test/ -q` and `node --check ros_graph_debugger/web/app.js` -->

- [ ] `pytest test/` passes
- [ ] `node --check ros_graph_debugger/web/app.js` passes (if web changed)
- [ ] Docs / CHANGELOG updated if behaviour or the API surface changed

## Notes

<!-- Anything reviewers should know: trade-offs, follow-ups, screenshots. -->
